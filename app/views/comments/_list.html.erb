<ul>
  <% collection.each do |comment| %>
    <li>
      <%= comment.body %>
      Δημιουργήθηκε από: <%= comment.user.nickname %>
      Στις: <%= comment.created_at %>
      <%= link_to "Destroy", article_comment_path(comment.article, comment), :method => :delete if can? :destroy, comment %>
    </li>
  <% end %>
</ul>