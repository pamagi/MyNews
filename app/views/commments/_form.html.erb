<%= form_for(@commment) do |f| %>
  <% if @commment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commment.errors.count, "error") %> prohibited this commment from being saved:</h2>

      <ul>
      <% @commment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :article_id %><br />
    <%= f.text_field :article_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
